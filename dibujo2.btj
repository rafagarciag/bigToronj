drawing (1024, 768, #ffffff){

	trans(_WIDTH/2, _HEIGHT/2);
	
	push();
	set_color(#c8f1aa);
	int radio = 100;
	circle(radio);
	
	color black = #000000;
	set_color(black);

	int base = 50;
	int altura = 40;
	tetragon(base, altura);
	pop();
	
	base = altura + 1;
	altura = altura-1;
	
	if (base < altura && cosa >= 2){
		radio = altura * 2;
	}

}

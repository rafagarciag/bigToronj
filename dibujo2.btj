function int factorial(int x){
	int aux;
	printl("La x: ");
	printl(x);
	if(x<=1){
		aux=1;
		printl("Caso base!!");
	}
	else{
		printl("LLAMADA!");
		aux=x*factorial(x-1);
	}
	return aux;
}

drawing (500,500,#CAFEFE){
	factorial(5);
}
